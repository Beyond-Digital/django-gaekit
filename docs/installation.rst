============
Installation
============

At the command line::

    $ easy_install django-gaekit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-gaekit
    $ pip install django-gaekit